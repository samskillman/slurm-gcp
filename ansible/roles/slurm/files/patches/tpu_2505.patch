--- a/configure.ac
+++ b/configure.ac
@@ -489,6 +489,7 @@
 		 src/plugins/gres/gpu/Makefile
 		 src/plugins/gres/mps/Makefile
 		 src/plugins/gres/nic/Makefile
+ 		 src/plugins/gres/tpu/Makefile
 		 src/plugins/hash/Makefile
 		 src/plugins/hash/common_xkcp/Makefile
 		 src/plugins/hash/k12/Makefile
@@ -506,6 +507,7 @@
 		 src/plugins/job_submit/partition/Makefile
 		 src/plugins/job_submit/pbs/Makefile
 		 src/plugins/job_submit/require_timelimit/Makefile
+		 src/plugins/job_submit/tpu/Makefile
 		 src/plugins/jobacct_gather/Makefile
 		 src/plugins/jobacct_gather/cgroup/Makefile
 		 src/plugins/jobacct_gather/common/Makefile
--- a/slurm/slurm.h
+++ b/slurm/slurm.h
@@ -1533,6 +1533,7 @@
 	char *wckey;		/* Workload Characterization Key */
 	uint16_t x11;		/* X11 forwarding requested, 0=no, 1=yes */
 	char *x11_nodes;	/* list of nodes with x11 forwarding */
+	char *tpu_slice;	/* --tpu-slice option */
 } job_desc_msg_t;
 
 typedef struct job_info {
--- a/src/common/slurm_opt.c
+++ b/src/common/slurm_opt.c
@@ -178,6 +178,7 @@
 	{ "task-prolog",	required_argument, NULL, LONG_OPT_TASK_PROLOG },
 	{ "tasks-per-tres",	required_argument, NULL, LONG_OPT_TASKS_PER_TRES },
 	{ "test-only",		no_argument,	   NULL, LONG_OPT_TEST_ONLY },
+	{ "tpu-slice",		required_argument, NULL, LONG_OPT_TPU_SLICE },
 	{ "thread-spec",	required_argument, NULL, LONG_OPT_THREAD_SPEC },
 	{ "threads-per-core",	required_argument, NULL, LONG_OPT_THREADS_PER_CORE },
 	{ "time",		required_argument, NULL, 't' },
@@ -501,6 +502,14 @@
   .val = LONG_OPT_GPUS_PER_TASK,
   .usage = "number of gpus required per task",
   .reset_each_pass = true, },
+{ .name = "tpu-slice",
+  .arg_type = SLURM_OPT_STRING,
+  .arg_set_func = arg_set_tpu_slice,
+  .val = LONG_OPT_TPU_SLICE,
+  .usage = "Specify the TPU slice configuration",
+  .reset_each_pass = true,
+},
 { .name = "gres",
   .arg_type = SLURM_OPT_STRING,
   .arg_val = &opt_gres,
--- a/src/common/slurm_opt.h
+++ b/src/common/slurm_opt.h
@@ -180,6 +180,7 @@
 	LONG_OPT_TASKS_PER_TRES,
 	LONG_OPT_TEST_ONLY,
 	LONG_OPT_THREAD_SPEC,
+	LONG_OPT_TPU_SLICE,
 	LONG_OPT_THREADS_PER_CORE,
 	LONG_OPT_TIME,
 	LONG_OPT_TIME_MIN,
--- a/src/common/slurm_protocol_defs.c
+++ b/src/common/slurm_protocol_defs.c
@@ -1403,6 +1403,7 @@
 		xfree(msg->wait_all_nodes);
 		xfree(msg->wckey);
 		xfree(msg->x11_nodes);
+		xfree(msg->tpu_slice);
 		xfree(msg);
 	}
 }
--- a/src/common/slurm_protocol_pack.c
+++ b/src/common/slurm_protocol_pack.c
@@ -4233,6 +4233,7 @@
 	packstr(job_desc->wait_all_nodes, buffer);
 	packstr(job_desc->wckey, buffer);
 	pack16(job_desc->x11, buffer);
+	packstr(job_desc->tpu_slice, buffer);
 	packstr(job_desc->x11_nodes, buffer);
 }
 
@@ -4620,6 +4621,7 @@
 	safe_unpackstr(&job_desc->wait_all_nodes, buffer);
 	safe_unpackstr(&job_desc->wckey, buffer);
 	safe_unpack16(&job_desc->x11, buffer);
+	safe_unpackstr(&job_desc->tpu_slice, buffer);
 	safe_unpackstr(&job_desc->x11_nodes, buffer);
 	return SLURM_SUCCESS;
 }
--- a/src/plugins/gres/Makefile.am
+++ b/src/plugins/gres/Makefile.am
@@ -1,3 +1,3 @@
 # Makefile for gres plugins
 
--SUBDIRS = common gpu nic mps
+SUBDIRS = common gpu nic mps tpu
--- /dev/null
+++ b/src/plugins/gres/tpu/Makefile.am
+AUTOMAKE_OPTIONS = foreign
+
+PLUGIN_FLAGS = -module -avoid-version --export-dynamic
+
+AM_CPPFLAGS = -DSLURM_PLUGIN_DEBUG -I$(top_srcdir)
+
+pkglib_LTLIBRARIES = gres_tpu.la
+
+# GRES TPU plugin.
gres_tpu_la_SOURCES = gres_tpu.c
gres_tpu_la_LDFLAGS = $(PLUGIN_FLAGS)
gres_tpu_la_LIBADD = ../common/libgres_common.la
+
+force:
+$(gres_tpu_la_LIBADD) : force
+	@cd `dirname $@` && $(MAKE) `basename $@`
--- /dev/null
+++ b/src/plugins/gres/tpu/gres_tpu.c
+#include <ctype.h>
+#include <inttypes.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/types.h>
+#include <unistd.h>
+
+#include "slurm/slurm.h"
+#include "slurm/slurm_errno.h"
+
+#include "src/common/slurm_xlator.h"
+#include "src/common/bitstring.h"
+#include "src/common/env.h"
+#include "src/interfaces/gres.h"
+#include "src/common/list.h"
+#include "src/common/xstring.h"
+
+#include "../common/gres_common.h"
+
+const char plugin_name[] = "Gres TPU plugin";
+const char plugin_type[] = "gres/tpu";
+const uint32_t plugin_version = SLURM_VERSION_NUMBER;
+
+static list_t *gres_devices = NULL;
+
+static void _set_env(common_gres_env_t *gres_env)
+{
+	char *slurm_env_var = NULL;
+
+	if (gres_env->is_job)
+			slurm_env_var = "SLURM_JOB_TPUS";
+	else
+			slurm_env_var = "SLURM_STEP_TPUS";
+
+	gres_env->prefix = "tpu_";
+	gres_env->use_dev_num = true;
+
+	common_gres_set_env(gres_env);
+
+	if (gres_env->global_list) {
+		env_array_overwrite(gres_env->env_ptr, slurm_env_var,
+				    gres_env->global_list);
+		xfree(gres_env->global_list);
+	} else {
+		unsetenvp(*gres_env->env_ptr, slurm_env_var);
+	}
+}
+
+extern int init(void)
+{
+	debug("%s: %s loaded", __func__, plugin_name);
+	return SLURM_SUCCESS;
+}
+
+extern void fini(void)
+{
+	debug("%s: unloading %s", __func__, plugin_name);
+	FREE_NULL_LIST(gres_devices);
+}
+
+#include <dirent.h>
+#include <fcntl.h>
+
+static int _read_file(const char *filename, char *buffer, size_t max_len)
+{
+	int fd = open(filename, O_RDONLY);
+	if (fd < 0)
+		return -1;
+	ssize_t len = read(fd, buffer, max_len - 1);
+	close(fd);
+	if (len > 0 && buffer[len - 1] == '\n') {
+		len--;
+	}
+	if (len >= 0)
+		buffer[len] = '\0';
+	return len;
+}
+
+static const char *_tpu_device_id_to_name(const char *device_id_hex)
+{
+	if (strncasecmp(device_id_hex, "0x005e", 6) == 0)
+		return "v4";
+	if (strncasecmp(device_id_hex, "0x0063", 6) == 0)
+		return "v5e";
+	if (strncasecmp(device_id_hex, "0x0062", 6) == 0)
+		return "v5p";
+	if (strncasecmp(device_id_hex, "0x006f", 6) == 0)
+		return "v6e";
+	if (strncasecmp(device_id_hex, "0x0076", 6) == 0)
+		return "tpu7x";
+	return "unknown";
+}
+
+static list_t *_get_system_tpu_list(void)
+{
+	list_t *gres_list = list_create(destroy_gres_slurmd_conf);
+	DIR *dir = opendir("/sys/bus/pci/devices/");
+	if (!dir)
+		return gres_list;
+
+	struct dirent *entry;
+	while ((entry = readdir(dir))) {
+		if (entry->d_name[0] == '.')
+			continue;
+
+		char path[PATH_MAX];
+		char vendor_str[32] = {0};
+
+		snprintf(path, sizeof(path), "/sys/bus/pci/devices/%s/vendor", entry->d_name);
+		if (_read_file(path, vendor_str, sizeof(vendor_str)) <= 0)
+			continue;
+
+		if (strncasecmp(vendor_str, "0x1ae0", 6) != 0)
+			continue;
+
+		char device_str[32] = {0};
+		snprintf(path, sizeof(path), "/sys/bus/pci/devices/%s/device", entry->d_name);
+		_read_file(path, device_str, sizeof(device_str));
+
+		snprintf(path, sizeof(path), "/sys/bus/pci/devices/%s/iommu_group", entry->d_name);
+		char target[PATH_MAX];
+		ssize_t len = readlink(path, target, sizeof(target) - 1);
+		if (len <= 0)
+			continue;
+		target[len] = '\0';
+		char *group_id = strrchr(target, '/');
+		if (!group_id)
+			continue;
+		group_id++;
+
+		char cpulist[128] = {0};
+		snprintf(path, sizeof(path), "/sys/bus/pci/devices/%s/local_cpulist", entry->d_name);
+		_read_file(path, cpulist, sizeof(cpulist));
+
+		gres_slurmd_conf_t *gres = xmalloc(sizeof(gres_slurmd_conf_t));
+		gres->name = xstrdup("tpu");
+		gres->file = xstrdup_printf("/dev/vfio/%s", group_id);
+		gres->cpus = NULL;
+		gres->count = 1;
+		gres->type_name = xstrdup(_tpu_device_id_to_name(device_str));
+		gres->config_flags = GRES_CONF_AUTODETECT | GRES_CONF_HAS_FILE;
+		gres->plugin_id = gres_build_id("tpu");
+
+		list_append(gres_list, gres);
+	}
+	closedir(dir);
+
+	return gres_list;
+}
+
+extern int gres_p_node_config_load(list_t *gres_conf_list,
+				   node_config_load_t *config)
+{
+	int rc = SLURM_SUCCESS;
+
+	if (gres_devices)
+		return rc;
+
+	list_t *system_tpus = _get_system_tpu_list();
+	if (system_tpus && list_count(system_tpus) > 0) {
+		list_transfer(gres_conf_list, system_tpus);
+	}
+	FREE_NULL_LIST(system_tpus);
+
+	rc = gres_node_config_load(gres_conf_list, config, &gres_devices);
+
+	if (rc != SLURM_SUCCESS)
+		fatal("%s failed to load configuration", plugin_name);
+
+	return rc;
+}
+
+extern void gres_p_job_set_env(char ***job_env_ptr,
+			       bitstr_t *gres_bit_alloc,
+			       uint64_t gres_cnt,
+			       gres_internal_flags_t flags)
+{
+	common_gres_env_t gres_env = {
+		.bit_alloc = gres_bit_alloc,
+		.env_ptr = job_env_ptr,
+		.flags = flags,
+		.gres_cnt = gres_cnt,
+		.gres_devices = gres_devices,
+		.is_job = true,
+	};
+	_set_env(&gres_env);
+}
+
+extern void gres_p_step_set_env(char ***step_env_ptr,
+				bitstr_t *gres_bit_alloc,
+				uint64_t gres_cnt,
+				gres_internal_flags_t flags)
+{
+	common_gres_env_t gres_env = {
+		.bit_alloc = gres_bit_alloc,
+		.env_ptr = step_env_ptr,
+		.flags = flags,
+		.gres_cnt = gres_cnt,
+		.gres_devices = gres_devices,
+	};
+	_set_env(&gres_env);
+}
+
+extern void gres_p_task_set_env(char ***task_env_ptr,
+				bitstr_t *gres_bit_alloc,
+				uint64_t gres_cnt,
+				bitstr_t *usable_gres,
+				gres_internal_flags_t flags)
+{
+	common_gres_env_t gres_env = {
+		.bit_alloc = gres_bit_alloc,
+		.env_ptr = task_env_ptr,
+		.flags = flags,
+		.gres_cnt = gres_cnt,
+		.gres_devices = gres_devices,
+		.is_task = true,
+		.usable_gres = usable_gres,
+	};
+	_set_env(&gres_env);
+}
+
+extern void gres_p_send_stepd(buf_t *buffer)
+{
+	gres_send_stepd(buffer, gres_devices);
+}
+
+extern void gres_p_recv_stepd(buf_t *buffer)
+{
+	gres_recv_stepd(buffer, &gres_devices);
+}
+
+extern list_t *gres_p_get_devices(void)
+{
+	return gres_devices;
+}
+
+extern void gres_p_step_hardware_init(bitstr_t *usable_gres, char *settings)
+{
+	return;
+}
+
+extern void gres_p_step_hardware_fini(void)
+{
+	return;
+}
+
+extern gres_prep_t *gres_p_prep_build_env(
+	gres_job_state_t *gres_js)
+{
+	return NULL;
+}
+
+extern void gres_p_prep_set_env(char ***prep_env_ptr,
+				gres_prep_t *gres_prep, int node_inx)
+{
+	return;
+}
--- a/src/plugins/job_submit/Makefile.am
+++ b/src/plugins/job_submit/Makefile.am
@@ -7,4 +7,5 @@
 	lua \
 	partition \
 	pbs \
-	require_timelimit
+	require_timelimit \
+	tpu
--- /dev/null
+++ b/src/plugins/job_submit/tpu/Makefile.am
+AUTOMAKE_OPTIONS = foreign
+
+PLUGIN_FLAGS = -module -avoid-version --export-dynamic
+
+AM_CPPFLAGS = -DSLURM_PLUGIN_DEBUG -I$(top_srcdir)
+
+pkglib_LTLIBRARIES = job_submit_tpu.la
+
+job_submit_tpu_la_SOURCES = job_submit_tpu.c
+job_submit_tpu_la_LDFLAGS = $(PLUGIN_FLAGS)
--- /dev/null
+++ b/src/plugins/job_submit/tpu/job_submit_tpu.c
+#include <inttypes.h>
+#include <stdio.h>
+#include <string.h>
+#include <sys/types.h>
+#include <unistd.h>
+
+#include "slurm/slurm_errno.h"
+#include "src/common/slurm_xlator.h"
+
+#include "src/common/log.h"
+#include "src/common/xmalloc.h"
+#include "src/common/xstring.h"
+#include "src/slurmctld/slurmctld.h"
+
+const char plugin_name[] = "Job submit tpu plugin";
+const char plugin_type[] = "job_submit/tpu";
+const uint32_t plugin_version = SLURM_VERSION_NUMBER;
+
+static int _process_tpu_slice(job_desc_msg_t *job_desc, char **err_msg)
+{
+	if (!job_desc->tpu_slice)
+		return SLURM_SUCCESS;
+
+	char *tpu_str = xstrdup(job_desc->tpu_slice);
+	char *colon = strchr(tpu_str, ':');
+
+	if (!colon) {
+		error("job_submit/tpu: Invalid tpu-slice format (%s). Expected [TYPE]:[TOPOLOGY]", job_desc->tpu_slice);
+		if (err_msg)
+			*err_msg = xstrdup("Invalid tpu-slice format. Expected [TYPE]:[TOPOLOGY]");
+		xfree(tpu_str);
+		return SLURM_ERROR;
+	}
+
+	*colon = '\0';
+	char *tpu_type = tpu_str;
+	char *tpu_topology = colon + 1;
+
+	info("job_submit/tpu: DEMO LOG: Received slice request %s:%s", tpu_type, tpu_topology);
+
+	int x = 0, y = 0, z = 0;
+	uint32_t num_nodes = 0;
+	uint32_t num_chips = 0;
+	uint32_t tres_count = 1;
+
+	if (xstrcmp(tpu_type, "v6e") == 0) {
+		if (sscanf(tpu_topology, "%dx%d", &x, &y) == 2 ||
+		    sscanf(tpu_topology, "%d_%d", &x, &y) == 2) {
+			num_chips = x * y;
+			num_nodes = num_chips / 4;
+			tres_count = (num_chips < 4) ? (num_chips == 0 ? 1 : num_chips) : 4;
+			if (num_nodes > 0 && num_nodes <= 64) {
+				job_desc->min_nodes = num_nodes;
+				job_desc->max_nodes = num_nodes;
+			} else {
+				info("job_submit/tpu: FAILURE: Invalid v6e topology %s. Allowed sizes up to 16x16 (64 nodes).", tpu_topology);
+				if (err_msg) *err_msg = xstrdup_printf("Invalid v6e topology %s.", tpu_topology);
+				xfree(tpu_str);
+				return SLURM_ERROR;
+			}
+		} else {
+			info("job_submit/tpu: FAILURE: Invalid v6e format %s. Expected 2D (XxY).", tpu_topology);
+			if (err_msg) *err_msg = xstrdup_printf("Invalid v6e format %s. Expected 2D (XxY).", tpu_topology);
+			xfree(tpu_str);
+			return SLURM_ERROR;
+		}
+	} else if (xstrcmp(tpu_type, "tpu7x") == 0) {
+		if (sscanf(tpu_topology, "%dx%dx%d", &x, &y, &z) == 3 ||
+		    sscanf(tpu_topology, "%d_%d_%d", &x, &y, &z) == 3) {
+			num_chips = x * y * z;
+			num_nodes = num_chips / 4;
+			tres_count = (num_chips < 4) ? (num_chips == 0 ? 1 : num_chips) : 4;
+
+			if (num_nodes > 0) {
+				if (num_nodes == 1 || num_nodes == 2 || num_nodes == 4 || num_nodes == 8 || (num_nodes >= 16 && (num_nodes % 16 == 0))) {
+					job_desc->min_nodes = num_nodes;
+					job_desc->max_nodes = num_nodes;
+
+				} else {
+					info("job_submit/tpu: FAILURE: Invalid TPU v7x node count: %u. Allowed sizes are 1, 2, 4, 8, or multiples of 16.", num_nodes);
+					if (err_msg) *err_msg = xstrdup_printf("Invalid TPU v7x node count: %u. Allowed sizes: 1, 2, 4, 8, or multiples of 16.", num_nodes);
+					xfree(tpu_str);
+					return SLURM_ERROR;
+				}
+			} else {
+				info("job_submit/tpu: FAILURE: Invalid TPU v7x topology %s.", tpu_topology);
+				if (err_msg) *err_msg = xstrdup_printf("Invalid TPU v7x topology %s.", tpu_topology);
+				xfree(tpu_str);
+				return SLURM_ERROR;
+			}
+		} else {
+			info("job_submit/tpu: FAILURE: Invalid tpu7x format %s. Expected 3D (XxYxZ).", tpu_topology);
+			if (err_msg) *err_msg = xstrdup_printf("Invalid tpu7x format %s. Expected 3D (XxYxZ).", tpu_topology);
+			xfree(tpu_str);
+			return SLURM_ERROR;
+		}
+	} else {
+		info("job_submit/tpu: FAILURE: Unknown TPU generation %s.", tpu_type);
+		if (err_msg) *err_msg = xstrdup_printf("Unknown TPU generation %s.", tpu_type);
+		xfree(tpu_str);
+		return SLURM_ERROR;
+	}
+
+	if (job_desc->tres_per_node) {
+		char *new_tres = xstrdup_printf("%s,gres/tpu:%s:%u", job_desc->tres_per_node, tpu_type, tres_count);
+		xfree(job_desc->tres_per_node);
+		job_desc->tres_per_node = new_tres;
+	} else {
+		job_desc->tres_per_node = xstrdup_printf("gres/tpu:%s:%u", tpu_type, tres_count);
+	}
+
+	debug("job_submit/tpu: Computed tpu_slice %s -> tres_per_node=%s features=%s min_nodes=%u",
+	      job_desc->tpu_slice, job_desc->tres_per_node, job_desc->features, job_desc->min_nodes);
+
+	xfree(tpu_str);
+	return SLURM_SUCCESS;
+}
+
+extern int job_submit(job_desc_msg_t *job_desc, uint32_t submit_uid,
+		      char **err_msg)
+{
+	return _process_tpu_slice(job_desc, err_msg);
+}
+
+extern int job_modify(job_desc_msg_t *job_desc, job_record_t *job_ptr,
+		      uint32_t submit_uid, char **err_msg)
+{
+	return _process_tpu_slice(job_desc, err_msg);
+}
--- a/src/plugins/select/cons_tres/job_info.c
+++ b/src/plugins/select/cons_tres/job_info.c
@@ -2582,6 +2582,10 @@
 	if (job->details->tres_per_task)
 		select_g_tres_sprint(job->details->tres_per_task,
 				     buf, sizeof(buf), 0);
+	if (job->details->tpu_slice) {
+		xstrcat(ret_str, " TpuSlice=");
+		xstrcat(ret_str, job->details->tpu_slice);
+	}
 
 	if (select_g_job_req_tres_display_str(job, &tres_str)) {
 		xstrcat(ret_str, " TresPerJob=");
--- a/src/plugins/select/cray_aries/job_info.c
+++ b/src/plugins/select/cray_aries/job_info.c
@@ -119,6 +119,11 @@
 		xstrcat(ret_str, " MCS_label=");
 		xstrcat(ret_str, job->details->mcs_label);
 	}
+	if (job->details->tpu_slice) {
+		xstrcat(ret_str, " TpuSlice=");
+		xstrcat(ret_str, job->details->tpu_slice);
+	}
+
 	if (job->details->mem_per_tres) {
 		xstrcat(ret_str, " MemPerTres=");
 		xstrcat(ret_str, job->details->mem_per_tres);
--- a/src/salloc/opt.c
+++ b/src/salloc/opt.c
@@ -411,6 +411,11 @@
 "      --mem-per-gpu=n         real memory required per allocated GPU\n"
 		);
 
+	printf("\n"
+"TPU scheduling options:\n"
+"      --tpu-slice=...         TPU slice type and shape to allocate\n"
+		);
+
 	printf("\n"
 "Help options:\n"
 "  -h, --help                  show this help message\n"
@@ -440,7 +445,7 @@
 "              [--cpus-per-gpu=n] [--gpus=n] [--gpu-bind=...] [--gpu-freq=...]\n"
 "              [--gpus-per-node=n] [--gpus-per-socket=n] [--gpus-per-task=n]\n"
 "              [--mem-per-gpu=MB]\n"
-"              [command [args...]]\n");
+"              [--tpu-slice=type:shape] [command [args...]]\n");
 }
 
 int main(int argc, char **argv)
--- a/src/sbatch/opt.c
+++ b/src/sbatch/opt.c
@@ -619,6 +619,11 @@
 "      --mem-per-gpu=n         real memory required per allocated GPU\n"
 		);
 
+	printf("\n"
+"TPU scheduling options:\n"
+"      --tpu-slice=...         TPU slice type and shape to allocate\n"
+		);
+
 	printf("\n"
 "Help options:\n"
 "  -h, --help                  show this help message\n"
@@ -648,7 +648,7 @@
 "              [--cpus-per-gpu=n] [--gpus=n] [--gpu-bind=...] [--gpu-freq=...]\n"
 "              [--gpus-per-node=n] [--gpus-per-socket=n] [--gpus-per-task=n]\n"
 "              [--mem-per-gpu=MB]\n"
-"              executable [args...]\n");
+"              [--tpu-slice=type:shape] executable [args...]\n");
 }
 
 /*
--- a/src/srun/opt.c
+++ b/src/srun/opt.c
@@ -118,6 +118,7 @@
 char *opt_tres_per_task = NULL;
 char *opt_tres_bind = NULL;
 char *opt_tres_freq = NULL;
+char *opt_tpu_slice = NULL;
 
 /*
  * NOTE: The order of these options is important. They are ordered to permit
@@ -290,6 +291,10 @@
 	job_desc.tres_per_task = opt_tres_per_task;
 	job_desc.tres_bind = opt_tres_bind;
 	job_desc.tres_freq = opt_tres_freq;
+	if (opt_tpu_slice) {
+		job_desc.tpu_slice = opt_tpu_slice;
+	}
+
 	if (job_desc.user_id == 0)
 		job_desc.user_id = uid;
 
@@ -803,6 +808,11 @@
 "      --mem-per-gpu=n         real memory required per allocated GPU\n"
 		);
 
+	printf("\n"
+"TPU scheduling options:\n"
+"      --tpu-slice=...         TPU slice type and shape to allocate\n"
+		);
+
 	printf("\n"
 "Help options:\n"
 "  -h, --help                  show this help message\n"
@@ -832,7 +842,7 @@
 "            [--cpus-per-gpu=n] [--gpus=n] [--gpu-bind=...] [--gpu-freq=...]\n"
 "            [--gpus-per-node=n] [--gpus-per-socket=n] [--gpus-per-task=n]\n"
 "            [--mem-per-gpu=MB]\n"
-"            executable [args...]\n");
+"            [--tpu-slice=type:shape] executable [args...]\n");
 
 }
 
--- a/src/srun/srun.h
+++ b/src/srun/srun.h
@@ -100,6 +100,7 @@
 	char *resp_host;
 	char *select_jobinfo;
 	char *shared;
+	char *tpu_slice;
 	char *tres_bind;
 	char *tres_freq;
 	char *tres_per_job;
